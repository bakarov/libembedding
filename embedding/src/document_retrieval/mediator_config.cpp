#include "mediator_config.h"

MediatorConfig MediatorConfig::getMediatorConfig(const std::string& sourceLanguage, const std::string& targetLanguage) const {
    return MediatorConfig(); // TODO: replace
}

std::string MediatorConfig::searchMany(const std::string& supportDoc) const {
    return "test";
}